import { homepageSettings } from "./homepageSettings";
import { product } from "./product";
import { productCategory } from "./productCategory";
import { aboutCompanyCarousel, aboutImageGallery, aboutPage, aboutRecommendation } from "./about";

export const schemaTypes = [homepageSettings, productCategory, product, aboutPage, aboutRecommendation, aboutImageGallery, aboutCompanyCarousel];

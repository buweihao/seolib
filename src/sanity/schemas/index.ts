import { homepageSettings } from "./homepageSettings";
import { product } from "./product";
import { productCategory } from "./productCategory";

export const schemaTypes = [homepageSettings, productCategory, product];
